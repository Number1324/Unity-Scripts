using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fire : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Вывод в консоль приветствие мира
        print("Hello, world!!!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
